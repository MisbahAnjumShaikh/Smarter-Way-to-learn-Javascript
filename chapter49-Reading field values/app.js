function checkAddress(emailId){
    var emailId = document.getElementById(emailId).value;
    // Alert user to fill the field as required attribute
    if(emailId === ''){
        alert("Email address required.")
    }
    // else if(emailField === ){

    // }
}

function fieldFocus(focusField){
    // var fielddFocus = document.get
    focusField.style.color = "red" 
}